# GitHub Copilot instructions

Follow the repository root **`AGENTS.md`** for all work in this project.

Key points:
- KIP-1318 / KAFKA-20436 reference (Python); production target is Java
- PyPI: `kafka-mcp-enterprise`
- Keep `kafka_mcp/` stdlib-only; run `python run_tests.py` (85/85) after changes
- Skills: `skills/` (portable) and `.cursor/skills/` (Cursor)
